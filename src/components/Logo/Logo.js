import styled from 'styled-components';

import { COLORS } from '../../constants';

const Logo = styled.h1`
  font-size: ${44 / 16}rem;
  font-weight: 900;
  color: ${COLORS.black};
`;

export default Logo;
